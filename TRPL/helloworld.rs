fn main() { //defines a function named main, first funcion to be executed in the program
    println!("Hello world!"); // calls a rust macro, macros are a way to write code that generated
                              // code to exend rust syntax
                              // using a '!' means we use a macro, not a function.
}
